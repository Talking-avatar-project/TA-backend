def detect_intent_text(user_message):
    # Esta es una simulación de la respuesta de Dialogflow.
    # En un proyecto real, aquí se integraría el cliente de Dialogflow para enviar el mensaje y recibir una respuesta.
    # Puedes modificar este contenido para que devuelva resultados esperados de prueba.

    response = {
        "fulfillmentText": f"Simulación de respuesta para: {user_message}",
        "intent": "Simulated Intent"
    }
    return response
